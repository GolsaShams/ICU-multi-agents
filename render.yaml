services:
  - type: web
    name: icu-multi-agents-dashboard
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "cd icu-dashboard && gunicorn app:app -b 0.0.0.0:$PORT --workers 1 --threads 4"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
